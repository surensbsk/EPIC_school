[project]
name = "practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pdfplumber>=0.11.9",
    "requests>=2.33.1",
]

[tool.uv.workspace]
members = [
    "weather",
]
